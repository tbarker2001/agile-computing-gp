import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Render the "App" react component into the "root" HTMl element
ReactDOM.render(<App />, document.getElementById('root'));